# E07 — Active auditing (registered in docs/experiment_registry.md)
experiment: E07
inherits: [E01, E02, E05, E06]
models: ["A:qksvc", "A:rbf_svc", "A:xgboost", "B:xgboost"]
deltas: [-0.01, -0.005, 0.0, 0.02, 0.05, 0.10]
alpha: 0.05
n_max: 20000
budgets: [10, 20, 50, 100, 200, 500, 1000, 2000, 3000, 5000, 10000, 20000]
audit_seeds: 20
strategies:
  uniform: {}                       # baseline (E06 semantics, own seeds)
  uncertainty_mix:                  # q = 0.5*uniform + 0.5*closeness-to-threshold
    mix: 0.5                        # uniform floor -> importance weights <= 1/mix = 2
    eps_frac_of_median: 0.1         # u_i = 1/(|p_i - thr| + eps), eps = frac * median|p - thr|
seed_salt: "E07"
