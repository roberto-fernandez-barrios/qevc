# E06 — Partial-label certification landscape (registered in docs/experiment_registry.md)
# Streams over the E02 score archive; frozen thresholds and M_S from E05.
experiment: E06
inherits: [E01, E02, E05]
models: ["A:qksvc", "A:rbf_svc", "A:xgboost", "B:xgboost"]
deltas: [-0.01, -0.005, 0.0, 0.02, 0.05, 0.10]   # same predeclared claim grid as E05 v1.1
alpha: 0.05
n_max: 20000
budgets: [10, 20, 50, 100, 200, 500, 1000, 2000, 3000, 5000, 10000, 20000]
audit_seeds: 20
seed_salt: "E06"      # independent streams from E05 (own replication)
