# Research specification — canonical copy moved

The canonical, governing copy of the research specification lives at
**`docs/research_spec.md`** (it was byte-identical to the file that
previously occupied this path; the duplicate was collapsed to this pointer
by decision **D-030** — see `docs/decisions.md`).

Nothing else about the specification changed: it remains the frozen
blueprint referenced by the experiment registry, the statistical analysis
plan, and the decision log.
